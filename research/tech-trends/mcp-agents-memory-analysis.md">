# mcp-agents-memory 项目分析：韩国新兴 Agent 记忆系统

**发布日期**：2026 年 5 月 3 日
**最新版本**：0.9.0
**作者**：isaacyoon (a3lab01create-bit)
**GitHub**：github.com/a3lab01create-bit/mcp-agents-memory

---

## 📊 项目概况

### 基本信息

| 指标 | 数据 |
|-----|------|
| 项目年龄 | 极新（仅发布 3 天） |
| npm 包状态 | Public，无依赖，无依赖包（dependents） |
| 版本历史 | 已有 8 个版本 |
| 主要维护者 | isaacyoon（韩国开发者） |
| 背后机构 | A3 Lab（推测为 a3lab01create-bit 的实验室） |

### 定位

**mcp-agents-memory** 是一个以人类记忆为灵感的、供多个 Agent 共享的长期记忆 MCP 服务器。

**设计目标**：
- 多 Agent 会话间共享记忆池
- 时间导向的原始存储
- 标签系统的后期处理
- 热/冷路径架构的性能优化

---

## 🏗️ 核心架构

### 1. 双路径架构（Hot/Cold）

这是项目最显著的设计特点：

```
┌──────────────────┐        Hot Path
│  Agent (Claude)  │ → → 实时存储（立即写入）
│      /Codex      │     无需等待
└──────────────────┘
          │
          ▼
┌─────────────────────────────────────────────┐
│     memory 表（原始数据存储）                │
│  - user_id, agent_platform, message         │
│  - p_tag, d_tag, embedding 初始为空         │
│  - created_at 实时写入                      │
└─────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────┐
│  Cold Path（后台任务）                       │
│  1. Tagger（gemini-2.5-flash）             │
│     → 预定义标签 + 动态标签                 │
│  2. Embedder（text-embedding-3-large）    │
│     → 向量嵌入（3072 维）                   │
│  3. Librarian（核心用户信息提取）            │
└─────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────┐
│   user 表，project_tags 表（后期更新）       │
└─────────────────────────────────────────────┘
```

**设计决策**：
- **Hot Path**：保证 Agent 响应速度（用户体验优先）
- **Cold Path**：后台完成繁重工作（AI 标签、嵌入生成）

### 2. 核心数据模型

#### memory 表

| 字段 | 类型 | 用途 |
|-----|------|-----|
| `user_id` | 字符串 | 用户唯一标识 |
| `agent_platform` | 枚举 | claude-code / codex / chatgpt / hermes-agent / openclaw |
| `agent_model` | 字符串 | 如 opus-4-7 / gemini-3-pro / gpt-5.5 |
| `subagent` | 布尔 | 追踪子代理身份 |
| `role` | 枚举 | user / assistant |
| `message` | 长文本 | 原始消息内容 |
| `p_tag` | 字符串 | 预定义标签（Cold Path 生成） |
| `d_tag` | 字符串 | 动态标签（Cold Path 生成） |
| `embedding` | 向量(3072) | 嵌入向量（Cold Path 生成） |
| `is_pinned` | 布尔 | 强制记忆（archive 豁免） |
| `is_active` / `archived_at` | 布尔/时间戳 | 软删除（保留历史） |

#### user 表

Librarian 自动从 memory 中提取核心信息：
- `core_profile`：非常重要的用户核心信息
- `sub_profile`：其他需要记住的用户信息

#### project_tags 表

动态积累项目相关标签：
- 支持别名合并（alias_of 字段）
- 支持语义标签体系

### 3. 记忆加载规则

```
┌─────────────────────────────────────────────────────┐
│  短期记忆（2-3 天内）                               │
│    - 原始 raw 数据（无需压缩）                      │
│    - 或 8000 字符限制（约 1.5 页）                  │
│    - char/1.7 ≈ token 估算（快速近似）             │
└─────────────────────────────────────────────────────┘
                       │
┌─────────────────────────────────────────────────────┐
│  中期记忆（2 周内）                                 │
│    - 标签优先检索                                  │
│    - 重要内容提取摘要                              │
└─────────────────────────────────────────────────────┘
                       │
┌─────────────────────────────────────────────────────┐
│  长期记忆（归档）                                   │
│    - 软删除保留在内存表中                          │
│    - archived_at 标记                              │
└─────────────────────────────────────────────────────┘
```

---

## 🧭 设计理念与来源

### 1. 项目灵感来源

根据文档，mcp-agents-memory 主要受两个项目启发：

#### a. supermemory
- 项目：supermemory.io（已停更？）
- 特点：语义图驱动的通用记忆层
- 借鉴点：语义检索、知识图谱结构

#### b. Hermes Agent
- 项目：Nous Research / Nous Hermes
- 特点：MEMORY.md 风格自动更新
- 借鉴点：技能/规则系统、自动文档

### 2. 与 v0.x 版本的重大重构

**RESPEC.md** 显示项目经历了重大架构变更：
- **旧版（v0.x）**：fact_type 分类轴为中心
- **新版（v0.9.0）**：时间轴（时间 + 标签 + 嵌入）为中心

**原因**：旧版的 fact_type 分类与人类实际记忆方式不匹配。

---

## 🌍 与其他记忆方案对比

### MCP 记忆生态现状（2026-05）

搜索发现当前有多个活跃的 MCP 记忆项目：

| 项目 | 特点 | 技术栈 |
|-----|------|--------|
| **mcp-agents-memory** | 热/冷路径架构、人类记忆模拟 | Node.js + SQLite + Gemini |
| **agentkits-memory** | AityTech 出品、多语言支持、Web 查看器 | better-sqlite3（原生极速） |
| **agentic-memory** (Brain) | 认知图、因果分析、时间演化追踪 | Rust（核心引擎） |
| **mcp-engram-memory** (Engram) | 频谱感知、聚类增强、MMR 重排 | C# / .NET |
| **agent-memory-mcp** (Adam Drew) | 混合 BM25+向量、本地嵌入（ONNX） | LanceDB |

### 设计权衡比较

| 维度 | mcp-agents-memory | 竞品特点 |
|-----|-------------------|---------|
| 性能优先度 | ⭐⭐⭐⭐ 高（热/冷分离） | 不同程度的性能考虑 |
| 结构记忆 | 标签系统+用户档案 | Graphiti-like 知识图 |
| AI 依赖 | Gemini 标签化、OpenAI 嵌入 | 不同程度 AI 介入 |
| 本地优先 | 数据本地存储 | 大多本地优先 |
| 跨平台共享 | 支持多个 Agent 模型 | 各有侧重 |

---

## ⚠️ 现状与局限性

### 当前状态

**项目新鲜度极高**：
- 发布仅 3 天（2026-05-03）
- 还在“fresh implementation”阶段
- npm 上 0 个 dependents（没有其他包依赖它）
- 暂未看到社区反馈或生产应用案例

### 已知局限性

1. **采用风险**：
   - 极新项目，未经生产验证
   - 文档主要是韩文，对中文/英文用户不友好
   - 缺少社区支持

2. **技术依赖**：
   - 依赖 Gemini（标签化）和 OpenAI（嵌入）
   - 双路径架构复杂度增加

3. **实际案例缺失**：
   - 搜索不到生产环境应用
   - 无权威评测或对比数据

---

## 💡 借鉴与启发

### 可以学习的设计

#### 1. 热/冷路径架构

这是最有价值的设计：
- **Hot Path**：即时存储，保证响应速度
- **Cold Path**：后台标签、嵌入、总结

这种分离设计适合用户体验要求高的场景。

#### 2. 人类记忆模拟

- 短期记忆：原始 raw
- 中期记忆：标签+摘要
- 长期记忆：归档（软删除）
- Librarian 自动提取用户画像

#### 3. 多 Agent 原生支持

- agent_platform 字段
- 同一用户不同 Agent 共享记忆池
- subagent 追踪

---

## 📝 实际应用建议

### 生产环境采用建议

**当前阶段**：
- ❌ 不推荐直接在生产环境采用
- ✅ 可以作为研究参考或 PoC
- ✅ 学习其设计理念

### 适合的场景

如果要尝试，适合的场景：
1. **科研/教育**：研究 Agent 记忆机制
2. **多 Agent 系统**：探索记忆共享设计
3. **非核心业务**：低风险的内部工具

### 可复用组件

即使不采用完整项目，也可以借鉴：
- 热/冷路径分离
- Librarian 概念（自动用户画像）
- soft delete 归档策略

---

## 🎯 后续关注

### 值得追踪的指标

1. **GitHub Stars** 增长曲线
2. **npm Downloads** 和依赖数
3. **Issues/PRs** 活跃度
4. **企业采用** 案例
5. **与 OpenClaw / Claude Code** 生态集成

### 潜在后续方向

- 可能加入更多 Memory as a Service 特性
- 可能发布英文文档
- 可能有更多 Agent 平台支持

---

## 📚 相关文档

- [本项目技术架构文档](./agent-architecture-mcp-memory.md)
- [企业落地指南](../applications/internal/enterprise-adoption-guide.md)
- [OpenClaw/Harness Engineering](./harness-engineering.md)

---

## 📌 结论

**mcp-agents-memory 是 2026 年最具创新性的 Agent 记忆方案之一**，设计理念先进（人类记忆模拟、热/冷路径），但采用需要谨慎。

对于**架构团队**来说：
- ✅ 值得研究和学习其设计
- ❌ 不建议在生产环境立即采用

**总结建议**：观察项目发展 1-3 个月，等社区反馈和稳定版本再决定。